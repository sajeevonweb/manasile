/** Score bands for trait/facet results (0–100 scale from Likert averages). */
export const SCORE_BAND_LOW_MAX = 30;
export const SCORE_BAND_HIGH_MIN = 70;

/**
 * Shown on results screens so users know scores are self-reported scale values,
 * not population percentiles.
 */
export const SCORE_SCALE_DISCLAIMER =
  'Scores are based on your own answers, converted to a 0–100 scale. They are not population percentiles. ' +
  'Labels such as lower, moderate, and higher tendency use fixed bands on that scale (0–30, 31–69, 70–100) as rough guides only.';
